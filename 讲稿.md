# 城市空气质量监测系统 · 讲解稿

> 录制时长：13-15 分钟
> 标记说明：
> - `【】` = 操作/切屏提示
> - `...` = 停顿 1-2 秒
> - `——` = 重音
> - `【关键】` = 必演示操作

---

## 第一段：开场（1 分钟）

【PPT 标题页】

大家好，今天我演示「城市空气质量监测系统」。

整个系统分**三个端**：
- **本地模拟器**——按设备型号生成空气质量数据
- **微信小程序**——给终端用户查询和绑定设备
- **Web 管理后台**——给运营管理人员做数据看板和客户管理

三端共享同一套后端服务，数据经 **Redis Stream** 缓冲后写入 **MongoDB** 存原始时序数据，每天定时聚合到 **MySQL** 做报表。

技术栈：**后端 Python Flask**，**前端 Vue3 + Element Plus**，**小程序原生框架**，**可视化 ECharts**，**AI 报告接入 DeepSeek**。

那我们从「数据从哪里来」开始看。

---

## 第二段：模拟器（2 分钟）

【切到本地终端】

这是**按设备型号驱动**的本地模拟器，核心配置文件 `sim_config.json`。

定义了**三种设备型号**：
- **AQ-Basic-100**——基础款，测 AQI 和 PM2.5 两项
- **AQ-Pro-2000**——专业款，5 项污染物
- **AQ-Industrial-5000**——工业款，6 项传感器高精度

每种型号有自己的**参数范围**和**波动值**。比如工业款 AQI 80 到 200，PM2.5 40 到 120。

我跑一个**重庆全部主城区**的演示，3 轮。

【敲命令】

```bash
python sim_runner.py --city 重庆 --rounds 3 --interval 1
```

...

9 个区全部跑通，每台设备按型号生成数据，带上地理信息（重庆市/渝中区），通过 **HTTPS + API Key + MD5 校验**发到云端。

【切到服务端日志终端】

这是 Flask 的服务端日志 —— 看到一条条 **200** 响应。

为了验证接口，我用 curl 模拟一次上报：

```bash
curl -X POST https://47.109.191.13/api/air-quality \
  -H "X-API-Key: 111" -H "Content-Type: application/json" \
  -d '{"device_id":"DEV_CQ_001","data":{"AQI":95,"PM₂.₅":48,"NO₂":35,"SO₂":12,"O₃":55},"location":{"province":"重庆市","city":"重庆市","district":"渝中区"},"user":{"name":"演示用户","industry":"办公"}}'
```

返回 `{"code":200,"message":"success"}`，接口正常。

---

## 第三段：后端数据流（2 分钟）

【切到 VS Code】

数据进来之后怎么落库？

**后端用异步流水线**：

1. **Flask** 收到请求，**不直接写库**，塞到 **Redis Stream**
2. **consumer.py** 独立进程，用 `XREADGROUP` 读 Stream
3. 写到 **MongoDB** `air_quality.records` 集合
4. **每天凌晨 01:05**，**daily_stats_job.py** 聚合，写 MySQL `daily_summary`

好处是**削峰 + 读写分离**。API 只管收，存库异步，consumer 挂了 Stream 数据不丢。

【打开 `alert_checker.py`】

这个**告警检测引擎**每 5 分钟扫一次 MongoDB，对比 `alert_rules` 表的阈值，超标的写 `alert_records` 并通知管理员。

【打开 MongoDB 可视化 — MongoMini】

```html
https://baimeixiaofan.xyz/mongo/
```

admin / admin123 登录进去，可以看到 `air_quality` 库，三个主要集合：**records**（~97MB 原始数据）、**daily_summary**（日聚合）、**devices**。点 records 进去，能直接看到最近 20 条上报数据，每条带 device_id、AQI、PM₂.₅ 等字段。

【打开 MySQL 可视化 — phpMyAdmin】

```html
https://baimeixiaofan.xyz/db/
```

同样 admin / admin123 登录，MySQL 连接用 `air_user` / 密码留空。进去看 `users` 表 —— 57 条用户，`devices` 表 —— 163 台设备，`alert_rules` 表 —— 11 条告警规则。

---

## 第四段：微信小程序（3 分钟）

【切到微信开发者工具 + 真机扫码】

现在演示小程序。**真机预览**，手机扫码打开。

【手机屏幕共享】

进入登录页。**强制要求完善资料**才能登录：
- 头像——`chooseAvatar` 组件
- 昵称——`type="nickname"` 自动拉取
- 手机号——两种方式：点「获取手机号」微信授权，或点「手动输入手机号」自己填

【关键】点「获取手机号」—— 弹出微信授权框，点同意，手机号自动绑上，不用手动输入。

【点「完成并登录」】

跳到首页。

【演示首页】

当前 AQI 卡片 + 设备列表。点「客厅」进去——

【进设备详情】

实时数据：AQI 76，PM₂.₅ 30，NO₂ 32。底下近 24 小时趋势图，ECharts 渲染。

【切告警 tab】

可以自定义阈值，保存后数据超了会收到订阅消息。

---

## 第五段：Web 管理后台（3 分钟）

【切到浏览器】

```html
https://baimeixiaofan.xyz/admin/
```

admin 登录 + 验证码。

**数据看板** —— 设备数、客户数、告警规则数、当天新增数据量。

【点「中国分布」】

地图放大到**重庆**，看到 9 个区的设备分布和平均 AQI。

【点「用户管理」】

**统一管理所有用户**——微信用户和 CRM 客户一个表搞定，用 `source` 字段区分。tab 切换看企业客户或小程序用户。

【点「告警规则」→「告警记录」】

切换到老监控面板手动触发报警。

【切到 `https://47.109.191.13/monitor`】

红色「报警演示」卡片，输入 `DEV_CQ_001`，点「触发报警」。

...

后端向 MongoDB 插一条 AQI=300 的超标数据。

【切回管理后台 → 告警记录刷新】

等几分钟刷新，看到这条严重告警。

【点「智能报告」】

选企业客户 → 月度报告 → 生成。

...

报告包含：
- 数据来源块：客户名、设备数、数据区间
- 3 张图表：AQI 趋势、污染物对比、等级分布环形图
- 3 张表格：逐设备分析、日均对比、超标统计
- AI 分析文字：DeepSeek 根据客户行业生成

可以直接导出 PDF，封面 + 数据 + 图表 + AI 分析一起，发给客户。

---

## 第六段：关键代码讲解（1.5 分钟）

【切到 VS Code】

讲**三个核心模块**：

**第一个，登录 API**（`miniprogram_api.py` 的 `/api/login`）

调微信 `jscode2session` 换 open_id，写入 **users 表**。用户表是统一的，微信用户和 CRM 客户用 `source` 字段区分，手机号支持微信授权或用 `/api/user/profile` 手动写入。

**第二个，告警检测引擎**（`alert_checker.py` 的 `check_alerts`）

读启用规则 → 转 MongoDB 条件 → 查最近 5 分钟 → **5 分钟内同设备同规则去重** → 写 `alert_records` → 发邮件。

**第三个，小程序登录页**（`flash-app/pages/login/login.js`）

`chooseAvatar` 选头像 → `type="nickname"` 填昵称 → 手机号二选一：`getPhoneNumber` 授权或手动输入 → 登录时自动调 `/api/user/bind-phone` 或 `/api/user/profile` 同步手机号。

---

## 第七段：部署架构 + 总结（1.5 分钟）

【PPT 部署架构图】

部署在**阿里云 ECS** Ubuntu，所有进程 **systemd 管理**：
- **Nginx**（443）—— HTTPS + 反向代理 + 静态文件
- **Flask**（5000）—— `flask-api.service` 自动重启
- **consumer.py / daily_stats_job.py** —— 独立进程
- **MongoMini**（8082）—— MongoDB Web 可视化
- **phpMyAdmin**（PHP-FPM）—— MySQL Web 可视化
- **MongoDB / MySQL / Redis** —— systemd 启动

【切到 SSH】

```bash
systemctl status flask-api mongomini
```

两个 active (running)。

```bash
ps aux | grep -E 'consumer|stats_job' | grep -v grep
```

后台进程都在。

**SSL 证书用 Let's Encrypt**，自动续期，域名已通过 ICP 备案。

**最后总结**：

本系统通过 **Redis Stream 削峰**，**MongoDB 存原始时序**，**MySQL 存报表**；告警引擎每 5 分钟扫超标数据，**DeepSeek AI 自动生成企业报告**；前端三端协同，形成「采集 → 存储 → 分析 → 告警 → 报告」完整闭环。

【停顿 2 秒】

我的演示到此结束，谢谢大家！

---

## 录制提示

1. **时长控制**：13 分钟 ± 1 分钟
2. **每段开头**留 1 秒停顿方便剪辑
3. **重点段**（数据流、AI 报告）可重复讲一遍要点强化
4. **不要念代码**——边指边说「这一行处理的是 XX」
5. **窗口切换**要流畅，提前排练几次

## 备用话术

- **如果 curl 报错**：「换 sim_runner 直接跑」
- **如果 API 500**：「网络抖动等一下重试」，趁机讲异常处理
- **如果 MongoMini 卡了**：「刷新一下，这个是轻量版偶尔会连超时」
- **如果 AI 报告超时**：「AI 推理需要时间，先看功能，回头回来看结果」
